export interface CriarUser {
    nome: string
    email: string
    senha: string
    telefone: number
    nascimento: string
    tipo: string
    ativo: boolean
}

export interface User {
    id: string
    nome: string
    email: string
    senha: string
    telefone: number
    nascimento: string
    tipo: string
    ativo: boolean
}